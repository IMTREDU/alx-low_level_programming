#include <stdio.h>
/**
 * main - It is the entry point
 *
 * Return: It will always return 0
 */
int main(void)
{
	int n;

	for (n = 0 ; n <= 9; n++)
	{
		printf("%d", n);
	}

	printf("\n");

	return (0);
}
