#include "main.h"
/**
 * main - It is the main
 * @argc: Num of argument
 * @argv: Num of argument(Array)
 * Return: Return Void
 */
int main(int argc, char *argv[])
{
	if (argc >= 1)
	{
		printf("%s\n", argv[0]);
	}

	return (0);
}
